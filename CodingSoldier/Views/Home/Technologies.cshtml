
@{
    ViewBag.Title = "Technologies Used - Coding Soldier";
}
<h2>CodingSoldier History:</h2>
<ul>
    <li>Website was initially developed in ASP.Net MVC with SQL server as database and deployed in IIS.</li>
    <li>Migrated to Node with Express and MongoDB as database.</li>
    <li>Migrated to Node with React and MongoDB as database.</li>
    <li>Migrated to ASP.Net Core with SQLite as database, hosted in a Linux instance on Kestrel server and Nginx as Reverse Proxy.</li>
    <li>Same technology of ASP.Net Core with SQLite as database, but hosted in AWS Lambda.</li>
</ul>
<br/>

<h2>Technologies currently used for CodingSoldier:</h2>
<ul>
    @foreach (var technology in ViewBag.technologies)
    {
        <li>@technology</li>
    }
</ul>
<br/>
<h2>Patterns and Practices followed in CodingSoldier:</h2>
<ul>
    @foreach (var pp in ViewBag.patternsAndPractices)
    {
        <li>@pp</li>
    }
</ul>




